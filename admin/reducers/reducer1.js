import * as TYPE from '../libs/const';

const initState = {

};

function draft(state = initState, action) {
    switch (action.type) {
        default:
            return state;
    }
}

export default draft;